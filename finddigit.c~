#include<stdio.h>
int main()
{
	int t;
	float n;
	scanf("%d",&t);
	int i;
	for(i =0 ;i<t;i++)
	{
		scanf("%f",&n);
		float m=n;
		float count = 0;		
		while(m>0)
		{
			float temp = m%10;
			m=m/10;
			if(n%temp==0)
				count++;
		}
		printf("%f",count);
		printf("\n");
	}
	return 0;
}
		
